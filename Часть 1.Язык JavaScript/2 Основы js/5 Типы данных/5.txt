let name = "Ilya";

alert( `hello ${1}` ); 

hello 1- т.к. в фигурных скобках стоит число

alert( `hello ${"name"}` ); 
hello name - т.к. в фигурных скобках стоит строка

alert( `hello ${name}` ); 
hello Ilya - т.к. в фигурных скобках стоит переменная и выведеться ее значение